// reverse - 부수효과 있음
// 원본 배열의 순서를 반대로 뒤집는다
// 원본이 변경된다
// 반환값은 변경된 배열이다

// 27-76
const arr1 = [1, 2, 3];
const result1 = arr1.reverse();

console.log(arr1); // [1, 2, 3]
console.log(result1); // [3, 2, 1]
